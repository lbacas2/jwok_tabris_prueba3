
jsw.qx.Class.define( "jsw.widgets.sidebar.SideBarGroup", {

  extend : jsw.widgets.base.Parent,
  
  include: jsw.widgets.mixin.SideBarItem,

  construct : function( styles ) {
    this.base( arguments );
  },

  members : {
  }
});
