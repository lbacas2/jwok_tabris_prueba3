
jsw.qx.Class.define( "jsw.widgets.menu.MenuSeparator", {

  extend : jsw.widgets.base.JSWItem,
  
  include: jsw.widgets.mixin.MenuItem,

  construct : function( styles ) {
    this.base( arguments );
  },

  members : {
  }
});
