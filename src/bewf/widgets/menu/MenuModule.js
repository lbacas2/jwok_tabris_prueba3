
jsw.qx.Class.define( "jsw.widgets.menu.MenuModule", {

  extend : jsw.widgets.base.Parent,

  include: jsw.widgets.mixin.MenuItem,

  construct : function( styles ) {
    this.base( arguments );
  },

  members : {
  }
});
