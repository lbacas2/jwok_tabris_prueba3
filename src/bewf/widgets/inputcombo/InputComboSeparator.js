jsw.qx.Class.define( "jsw.widgets.inputcombo.InputComboSeparator", {

  extend : jsw.widgets.base.Parent,
  
  include: jsw.widgets.mixin.InputComboItem,

  construct : function( styles ) {
    this.base( arguments );
  },

  members : {
  }
});
